# Blog — plano atual

## Hero e desempenho

- Hero e métricas usam fluxo vertical; preservar o nome inteiro e métricas legíveis de 320 px a desktop.
- O hero usa uma escultura metálica do monograma: duas molduras contínuas em L e seis pontes, com volume e bordas arredondadas. A coreografia de 18 segundos apresenta o símbolo, separa e gira as peças, recompõe e repousa. Movimento autônomo, sem botão ou ação necessária; manter o símbolo reconhecível durante os momentos de repouso. Abaixo de 1024 px, a figura entra no fluxo entre o nome e a apresentação.
- A cena usa 11 draw calls, 4200 triângulos e 90 partículas suaves. Antialiasing e DPR até 1,5 no mobile/toque/hardware com até quatro núcleos, e até 2 no desktop; não sacrificar a nitidez das bordas voltando ao DPR 1 em telas densas. Os materiais físicos refletem um estúdio procedural convertido uma vez em um mapa PMREM de 256 px. Não há download de HDR, bloom, sombras em passes extras ou blur de viewport. O grain pertence ao fundo, sem sobrepor o metal.
- `HeroArtwork` observa a própria visibilidade, o estado da aba e `home-locked`; `HeroCanvas` usa um loop sob demanda. Pausar fora da tela e sob o menu, preservar o instante da cena e limitar o delta ao retomar. Mudanças de tema e resize ainda podem desenhar um quadro.
- A inicialização aguarda visibilidade e um intervalo ocioso; o SVG fica presente enquanto os materiais compilam com `compileAsync`. Depois de iniciada, a cena continua montada sob o menu para evitar recompilação na reabertura. Movimento reduzido apresenta o monograma 3D montado e estático. Carregamento, ausência ou perda de WebGL usam o símbolo vetorial original, também sem controle. Não tratar todo celular como movimento reduzido.
- Os materiais Three compartilham os mesmos uniforms diretamente: R3F 9.6 copia os wrappers numéricos ao receber uniforms por JSX, o que congelava a deformação quando o código atualizava somente o objeto original. Descartar geometrias e materiais ao desmontar.
- No mobile, preservar as entradas do nome, textos, botões e seções. Scramble, parallax e fade do conteúdo continuam restritos ao desktop compatível. O relógio atualiza somente seu próprio componente.
- Datas de publicações usam UTC explicitamente para manter o HTML do servidor e do navegador idênticos.

## Experiência da home

- O CTA principal abre o Resilience Lab: uma simulação local de fila limitada, falha de workers, recuperação e aumento de capacidade. Manter explícito que o estado fica em memória e não representa telemetria de produção.
- A lógica está em `lib/architecture-lab.ts`; a interface deve refletir esse estado. Cada evento enviado precisa estar concluído, em processamento, na fila ou recusado. Falhas devolvem trabalho não confirmado; a recuperação não duplica commits.
- O laboratório só avança após ação do visitante, enquanto estiver visível e a aba estiver ativa. Respeitar movimento reduzido e manter os controles utilizáveis por toque e teclado.
- Os previews dos projetos ilustram as arquiteturas descritas nos respectivos MDX. Atualizar esses mapas quando os estudos de caso mudarem; não apresentar diagramas ilustrativos como screenshots de produção.
- O Architecture Decision Room em `#decision-room` apresenta três cenários hipotéticos (produto, pagamentos e IA), com duas alternativas cada. Texto e diagramas vêm de `data/architecture-decisions.ts`; mostrar ganhos, custos e critérios para rever cada escolha, sem inventar resultados de clientes. Interações locais, sem chamadas de IA ou loops de animação.
- O toolbox em `#toolbox` usa categorias legíveis de IA/agentes, desenvolvimento e operação. Preservar a quebra natural dos textos e os controles por teclado; não voltar ao anel 3D com etiquetas sobrepostas.

## Menu e primeira interação

- O cabeçalho anima a largura real entre 208 px e 100% de um pai limitado a 1168 px. Não animar `max-width` até 1168 px numa barra limitada pela viewport: em 390 px isso fazia a expansão visível acabar em 45 ms. Preservar duração de 550 ms, easing, padding e limiar de scroll; o limite desktop fica no pai.
- Na medição local de produção em 390 px, os dois sentidos passaram a chegar a menos de 1 px do destino em aproximadamente 399/392 ms. Conferidos também 320, 768 e 1440 px, resize, inversão do scroll durante a animação, menu e origem da onda de tema. Medições em Chromium, sem alegar execução em iPhone físico.

- Manter a cortina, entrada em cascata, auroras em movimento, gradiente ativo e transformação do ícone. A cortina usa dois elementos com `translateY` opostos; evitar remontar e animar `clip-path` de uma viewport com filtros grandes no primeiro clique.
- O menu fica montado, mas fechado usa `inert`, `aria-hidden`, `visibility: hidden` e animações pausadas; relógio só atualiza aberto. Reabrir começa no topo. O callback de fechamento é estável para não reinstalar o bloqueio de scroll a cada atualização do cabeçalho.
- Links do overlay pré-carregam por intenção (mouse ou foco), sem pré-carregar todas as rotas quando o menu aparece. A medição local de produção em 390 px passou de 19 requisições extras na primeira abertura para zero; esse resultado não equivale a medir FPS em iPhone físico.
- A suavidade das auroras vem dos próprios gradientes, preservando o deslocamento sem filtros de blur de 110 px. Hover com desfoque pertence apenas a `(hover: hover) and (pointer: fine)`; toque não deve deixar links desfocados. Sublinhados animam escala e opacidade, com espaço para a tipografia, sem largura zerada e recorte do texto.
- Validado em Chromium de 320 a 1440 px: primeira abertura/reabertura, cortina e cascata em progresso, movimento das auroras, duas direções de tema, Escape, navegação por clique/teclado e fechamento após mudança de rota. A linha rosa relatada no Safari não foi reproduzida nesse navegador; confirmar a correção de composição no aparelho.

## Tema e integração Spotify

- A onda de tema parte do centro medido do botão, inclusive por teclado e no cabeçalho compacto. Atualizar tema e ícone antes da captura; o ícone tem seu próprio `view-transition-name`. Cliques concorrentes são ignorados até terminar; movimento reduzido e navegadores sem View Transitions trocam diretamente.
- O sobrenome mantém folga horizontal no elemento com gradiente para não cortar a tinta do último glifo com tracking negativo.
- Credenciais do Spotify ficam exclusivamente no `.env` da VPS. A renovação de 2026-09-13 validou as chaves, renovou a autorização revogada e confirmou uma faixa no endpoint público. O retorno OAuth já cadastrado é `http://127.0.0.1:8888/`, usado pelo helper. Trocar o client secret não recupera um refresh token revogado.
- O container roda como `node` via Compose; manter `.next/cache` gravável por esse usuário na imagem para a otimização de imagens.
- Preservar proprietário, grupo e permissões do `.env` ao rotacionar credenciais. Na VPS, `leo:docker` com modo `640` é o estado preservado e conferido pelo helper de deploy; mudanças de modo/proprietário exigem manutenção explícita. Backups e arquivos temporários da sincronização usam umask `077`.

## Validação

- Para mudanças no laboratório, executar `yarn test:lab` com Node 24+ e verificar no navegador rajadas, capacidade cheia, falha, recuperação, reset, teclado e pausa fora da viewport.
- Antes de publicar mudanças no hero, executar `yarn build` e verificar o build de produção no navegador em 320, 390, 768 e 1440 px.
- Conferir nome, métricas, scroll, botão principal, resize e movimento reduzido. Verificar hidratação em UTC e America/Sao_Paulo.
- Para alterações nas novas experiências, verificar as seis combinações do Decision Room, as três categorias do toolbox e seus textos em 320, 390, 768 e 1440 px. Testar tema nas duas direções, por clique e teclado, com cabeçalho aberto e compacto; conferir a origem real da onda e o ícone durante a transição.
- Monograma conferido no build de produção em 320, 390, 768, 1024 e 1440 px, nos dois temas, montado e separado. Verificar o ciclo automático completo, a silhueta nos extremos e ausência de controles antigos. Movimento reduzido e fallback foram exercitados por parâmetros locais de teste, removidos antes da publicação; isso não equivale a mudar a preferência do sistema operacional ou provocar uma perda real do contexto WebGL.
- No Chromium local, com DPR 2, uma janela estável de 240 quadros registrou mediana de 16,6 ms e p95 de 19,2 ms. Em 390 px com DPR 1,5, janelas estáveis registraram mediana de 16,7 ms e p95 de 18,9–19,5 ms. O contador parou em 1337 sob o menu e em 1354 fora da seção; a retomada preservou o tempo. Movimento reduzido ficou em um quadro, desenhando apenas mais um na troca de tema. Instrumentação removida antes de publicar. Essas medidas não representam desempenho em iPhone físico, GPU móvel ou latência total da primeira interação.
- O lint legado está bloqueado pela configuração FlatCompat / Next 16; a tentativa com configuração nativa também falhou em minimatch/brace-expansion das dependências existentes. Não declarar lint verde com base no build; alinhar a correção do tooling em uma tarefa própria.
- Build e health check do workflow não substituem verificação visual. A verificação atual usou Chromium em larguras de celular; desempenho em iPhone físico ainda depende de validação no aparelho.

## D2 — contrato de publicação aplicado

- Blog **0.1.0** implantado em 14/09/2026, commit `01990f4d3d0b16fa5832b4340e4da477c9387e4e`, após CI `34864714015` e imagem qualificada no build `34865012930`. A [release v0.1.0](https://github.com/leodotsinc/blog/releases/tag/v0.1.0) registra o manifesto verificado e o digest. Este registro é histórico; confirmar a versão em produção antes de operar.
- Deploy depende da CI bem-sucedida do SHA atual de main. SemVer parte da última release publicada e verificada: mudanças comuns PATCH, `feat:` MINOR e breaking changes MAJOR. Tags/releases só são publicadas após health e identidade do runtime. Falha de publicação exige reutilizar o artefato original; não reconstruir outra imagem com a mesma versão nem sobrescrever tags.
- A chave dedicada chama o gateway Cloudbox comum com app fixo; o token GHCR curto
  segue por stdin e o host apaga o `DOCKER_CONFIG` temporário após o pull. O helper
  específico continua dono de health, checkpoint e rollback. O bootstrap inicial
  foi concluído; não repeti-lo. Operação e recuperação pertencem ao `cloudbox-infra`.
- O transporte fixa o workflow compartilhado por SHA e conserva erros públicos e
  checkpoints em artifact por execução/tentativa, inclusive em falha do gateway.
  A publicação consome `rollout.outputs.result_artifact`. Timeout/desconexão não
  comprovam rollback: conferir o runtime antes de repetir o manifesto original.
- Após a publicação, o workflow compartilhado de reconciliação envia esse recibo
  pela GitHub App `cloudbox-release-reconciler` e aguarda o PR/check/merge
  allowlisted no `cloudbox-infra`. Falha nessa etapa não apaga a prova de produção:
  corrigir a reconciliação sem reconstruir ou inventar outra identidade.
- `/api/health` não chama Spotify; `/api/version` expõe apenas versão, commit e build, com no-store. A validação real incluiu health/version públicos, Spotify, hero em 1440/390 px, menu e navegação. O teste de repetição pela chave restrita preservou o container; isso não é uma execução de rollout em runner GitHub nem restore independente.
- Next 16.3.3, Sharp 0.35.4 e libheif 1.23.2 foram qualificados na imagem; PNG/JPEG e `/_next/image` exercitados. A troca preservou hero, conteúdo e credenciais. O aviso de depreciação `THREE.Clock` já existia antes; o lint mantém a limitação acima.
- `scripts/sync-spotify-env.sh` está aposentado e recusa antes de rede/escritas. Rotação de credenciais exige manutenção própria com lock, checkpoint e validação, sem versão fictícia do app; o helper de refresh token foi preservado.

## Qualified maintenance adoption (cloudbox-infra PR 11)

- Mend Renovate installation was verified on 2026-09-21. The shared `leodotsinc/.github` preset owns npm/lockfile, Dockerfile and workflow discovery when this branch is merged; overlapping Dependabot version jobs are removed in the same change. Dependabot vulnerability alerts remain enabled. No AI reviewer or model call is added.
- PR and release image builds scan the immutable runtime and builder image IDs with pinned Trivy, a fresh database and bounded seven-day sanitized evidence. Unknown/stale results and high/critical findings refuse publication; passing source tests alone is insufficient.
- The ordinary release gate inspects all commits since the last verified published release, dependency/runtime/workflow paths and GitHub associated-PR metadata. Unqualified maintenance cannot reach production through a squash merge or a later feature commit. Read-only image preparation remains available; there is no calendar or label bypass.
- This repository's maintenance is not yet qualified for autonomous deployment. Cloudbox must bind the reviewed candidate to its policy, approved window, backup and runtime acceptance before enabling the dedicated executor. These changes do not prove installation or a production maintenance pilot.

- `scripts/maintenance-yarn-gate.py` now provides a read-only Yarn Classic source classifier using Git objects and a complete baseline-to-candidate tree diff. Only package/lock changes qualify for source review; scripts/features, Docker/workflow changes, unknown syntax/origins, lock drift, major/downgrade and ambiguous graphs refuse. Changed registry versions require matching SRI/graph, unchanged lifecycle/exports/engines, at least14days since official publication, fresh official advisory queries and exact complete CI. Registry packuments are bounded to 16 MiB each and 64 MiB of JSON per collector; GitHub responses remain capped at 4 MiB, with 65 calls and 15-second request timeouts. These explicit limits accommodate the observed Hono/electron metadata sizes; unknown, oversized or stale results still refuse. No raw packument, extra parser dependency or model is retained.
- `.github/maintenance.json` and the Yarn source/entry modules define a closed preparation envelope with fixed scheduler identities, canonical receipt/request hashes and bounded TTL. The original read-only collector authenticates published baseline assets and Git/PR/CI/security/functional evidence without executing candidate scripts. `maintenance-execution.py` adds preparation, a request artifact reserved before one exact-head merge, and independently revalidated release preflights. Configuration remains disabled with policy/code/host qualification pins absent. No activation or production execution is claimed.
- Runtime source A, trusted producer B, Renovate head H and merge M remain distinct. A→B may change only `.github/maintenance.json`, whose raw SHA-256 must equal the scheduler's root-approved `control_sha256`; all executable/source files remain identical. B→H may change only qualified package/lock dependencies, and M must have parents B/H and exactly H's tree. The cumulative A→H classifier preserves actual identities and records the control delta separately. Initial cumulative adoption code/features still require an explicitly reviewed operational bootstrap; this is no bootstrap bypass.
- The maintenance caller owns the global production concurrency group across preparation and the reusable Deploy workflow. Only that trusted caller gets a per-run child group in Deploy, avoiding lock reacquisition; ordinary deployment keeps the global group and existing maintenance guard. The callee verifies the closed request, enabled configuration, approved hashes, source graph, official metadata, CI H, merge tree and current window before build and publication. CI H is source evidence; a new exact M image gets its own functional receipt/scans and immutable release manifest. The final host revalidates policy, live baseline, backups and qualification under its locks; preparation and merge never authorize deployment. The shared transport carries the bounded maintenance-context artifact.
- CI retains only the ≤128 KiB `blog-source-qualification-R-A/source-qualification.json` for 45 days; image/scan/functional artifacts remain seven days. Refresh reruns the original pull-request CI run, preserving its exact head/event and producing a new attempt-bound artifact; there is no additional dispatch input. It records real A/B/H identities, the raw control hash, cumulative classifier result and code hashes, or an explicit refusal. Consumers must authenticate the CI run/job/archive and compare live baseline/control/code pins; the artifact is source analysis only and contains no deployment authorization. Existing source tests still determine their original CI result. Official registry/advisory and host checks remain mandatory.
- A failed/ambiguous merge or interrupted executor retains the request reservation and requires reconciliation instead of an automatic retry. Later runs cannot reuse an expired request or stale baseline. Pausing/revoking policy or missing app/host qualification refuses execution. The local test suite exercises these branches with synthetic Git/HTTP/artifact data; it does not prove live deployment or host activation. Run `python3 -B -m unittest discover -s scripts -p 'test_release*.py'` and validate workflows with actionlint.

- Image qualification emits a sanitized `functional.json` receipt retained for seven days by both CI and Deploy preparation. The receipt binds the exact runtime image ID, version/revision/build, run/attempt, observation time and qualification harness hashes; scans consume that same image ID. A future trusted collector must authenticate GitHub run/artifact metadata, verify the downloaded archive digest and compare independently approved source/harness identities. The receipt covers isolated health/version, native PNG/JPEG and Next image behavior; it does not prove visual/Spotify acceptance, a production rollout or restore, and does not enable maintenance.

- The isolated qs maintenance candidate pins 6.16.0 and adds bounded regressions for upstream GHSA-4mjr-xmp4-gh2g and GHSA-x5fp-wj9c-mxmx. The lockfile adds only compatible side-channel patch dependencies; body-parser and OpenTelemetry are deliberately unchanged and their outstanding advisories remain review items. This candidate changes no content or runtime features and does not authorize deployment. A live pilot still needs the cumulative delta from the verified production release, exact image qualification, checkpoint/return review and authenticated Spotify plus browser acceptance.

- The qs pilot branch pins both Node stages to `sha256:dbaa92e5758cbbcf85d65d5403fdb530fe3442cbe8c6dbfb7ef23365450d5070`, the exact base recorded by production 0.1.4 build [35489697263](https://github.com/leodotsinc/blog/actions/runs/35489697263). This freezes the existing base rather than qualifying a new Node update; the live source/digest and cumulative dependency delta must still be verified before release.

- The bootstrap dependency branch bounds seven unreleased transitive choices to mature versions checked against official publication dates and advisories on 2026-09-21: baseline-browser-mapping 2.11.0, browserslist 4.28.7, fast-uri 3.1.6, node-releases 2.0.51, update-browserslist-db 1.2.3, electron-to-chromium 1.5.393 and Hono 4.13.5. The browser-data versions satisfy browserslist’s declared ranges. All other lockfile package identities, direct dependencies and application behavior fields remain unchanged. These are equal/newer than production, not a production downgrade. Existing cumulative TOML 3→4 and retry 0.3→0.4 adoption changes still need separate operational bootstrap review; this branch does not authorize a release or autonomous maintenance.

## Reviewed adoption bootstrap (inactive until personal review and host approval)

- `reviewed-bootstrap.yml` is a separate, bounded personal release path for the cumulative adoption from production source `82a49ac5e72b76d31044e1782d49abfb65258111`. The ordinary maintenance guard and monthly classifier are unchanged. Only personal actor/triggering actor ID22529012, exact reviewed source/tree/delta, completed original `prepare_only` run and its authenticated preparation receipt qualify; a missing receipt refuses older preparations.
- The review binds the complete file delta to a static allowlist; feature paths, package behavior changes, unreviewed major/0.x changes and new packages refuse. TOML3.0.0→4.2.0, retry0.3.1→0.4.3 and the humanfs/types0.15.0 graph addition are explicit adoption-only review items. Existing TOML/MDX, nesting-depth and qs regression tests remain mandatory CI. Every changed package still needs official SRI/publication at least14days old and fresh advisories; no urgent or age exception exists.
- The already production-qualified Node base digest remains fixed in both stages. `update-browserslist-db`1.2.3 retains its production version and integrity; its `resolved` URL is restored to the exact production npm URL after Yarn serialized a mirror URL. The same-version drift gate remains strict.
- The publisher downloads the original prepared manifest, scans and functional receipt, verifies artifact API digests and producer jobs, then pulls the prepared digest to bind its image ID. It never rebuilds or pushes a new image and preserves original build.id/attempt. Only the existing shared gateway receives those same manifest bytes. Host root-protected baseline/target approval, locks and one-shot journal are independent requirements; this PR neither installs nor enables them.
- An approved review expires within60minutes and requires15minutes left before handoff. Interruption or failure requires reconciliation, not a rerun: run_attempt>1 refuses, and the host one-shot journal blocks a new dispatch from replaying mutation. After rollout, ordinary immutable publication and infra reconciliation run. No prepare/bootstrap dispatch, production acceptance or monthly qualification is claimed by this source change.
